package com.hexvane.aetherhaven.patrol;

public enum PatrolWandMode {
    /** Add and remove patrol points, save routes. */
    Build,
    /** Assign or remove guards on a route. */
    Assign,
    /** Select and delete saved patrol routes. */
    Remove
}
